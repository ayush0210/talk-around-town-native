const React = require('react');
const {View} = require('react-native');

const SpinnerMock = props => React.createElement(View, props);

module.exports = SpinnerMock;
module.exports.default = SpinnerMock;
