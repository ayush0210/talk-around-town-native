const React = require('react');
const {View} = require('react-native');

const DateTimePickerMock = props => React.createElement(View, props);

module.exports = DateTimePickerMock;
module.exports.default = DateTimePickerMock;
